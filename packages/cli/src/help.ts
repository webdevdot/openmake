export const HELP_TEXT = `openmake — design platform CLI

Usage:
  openmake export-json <file.omk.json>
  openmake codegen <file.omk.json> <nodeId> --framework <FRAMEWORK> --out <dir>
  openmake new <name>
  openmake --help

Commands:
  export-json   Read an openmake document and print a summary.
  codegen       Generate code for a node's subtree in the given framework.
  new           Create a starter document with a sample auto-layout frame.
`;
